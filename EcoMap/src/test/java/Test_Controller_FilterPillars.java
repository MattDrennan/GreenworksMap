import com.GREENWORKS.controller.FilterPillars;

import org.junit.jupiter.api.Test;

public class Test_Controller_FilterPillars {
    @Test
    void test() {
        FilterPillars fp = new FilterPillars();
        String[] request = {"Food", "Transportation"};
        //TODO fix the Controller classes so that one or more methods controls business logic
        //fp.service(request, res);
    }
}
